
    function scrollByAmount(pixels) {
      window.scrollBy({
        top: pixels, 
        behavior: 'smooth' // Para que el desplazamiento sea suave
      });
    }

    function scrollByAmount2(pixels) {
      window.scrollBy({
        top: pixels, 
        behavior: 'smooth' // Para que el desplazamiento sea suave
      });
    }
    function scrollByAmount3(pixels) {
      window.scrollBy({
        top: pixels, 
        behavior: 'smooth' // Para que el desplazamiento sea suave
      });
    }
